<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ job.title }} - بوابتي للتوظيف</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --yemen-red: #ce1126;
            --dark: #2c3e50;
            --light: #f8f9fa;
        }
        
        body {
            background: var(--light);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .navbar {
            background: linear-gradient(135deg, var(--yemen-red), #a4001a) !important;
        }
        
        .yemen-flag {
            height: 20px;
            width: 30px;
            background: linear-gradient(to bottom, #ce1126 33%, #ffffff 33%, #ffffff 66%, #000000 66%);
            display: inline-block;
            margin: 0 10px;
            border-radius: 2px;
        }
        
        .job-header {
            background: linear-gradient(135deg, var(--yemen-red), #a4001a);
            color: white;
            padding: 40px 0;
            border-radius: 0 0 30px 30px;
        }
        
        .job-content {
            background: white;
            border-radius: 20px;
            padding: 30px;
            margin-top: -50px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            position: relative;
        }
        
        .company-card {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            border-right: 4px solid var(--yemen-red);
        }
        
        .btn-primary {
            background: linear-gradient(45deg, var(--yemen-red), #a4001a);
            border: none;
            border-radius: 25px;
            padding: 12px 30px;
            font-weight: 600;
        }
        
        .btn-success {
            background: linear-gradient(45deg, #28a745, #20c997);
            border: none;
            border-radius: 25px;
            padding: 12px 30px;
            font-weight: 600;
        }
        
        .badge-yemen {
            background: var(--yemen-red);
            color: white;
        }
        
        .job-meta {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 20px;
        }
        
        .quick-apply-btn {
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
        }
        
        .quick-apply-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.3);
        }
    </style>
</head>
<body>
    <!-- شريط التنقل -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">
                <i class="fas fa-door-open me-2"></i>
                بوابتي للتوظيف
                <span class="yemen-flag"></span>
            </a>
            
            <div class="navbar-nav me-auto">
                <a class="nav-link" href="/">الرئيسية</a>
                <a class="nav-link" href="/jobs">الوظائف</a>
            </div>
            
            <div class="navbar-nav">
                <a href="/company/login" class="nav-link">تسجيل الدخول</a>
                <a href="/company/register" class="nav-link">سجل شركتك</a>
            </div>
        </div>
    </nav>

    <!-- محتوى الصفحة -->
    <div class="job-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <h1 class="fw-bold mb-3">{{ job.title }}</h1>
                    <p class="mb-2 fs-5">
                        <i class="fas fa-building me-2"></i>{{ job.company_name }}
                    </p>
                    <div class="d-flex flex-wrap gap-3">
                        {% if job.location %}
                        <span class="badge bg-light text-dark fs-6">
                            <i class="fas fa-map-marker-alt me-1"></i>{{ job.location }}
                        </span>
                        {% endif %}
                        
                        {% if job.job_type %}
                        <span class="badge badge-yemen fs-6">{{ job.job_type }}</span>
                        {% endif %}
                        
                        {% if job.category %}
                        <span class="badge bg-info fs-6">{{ job.category }}</span>
                        {% endif %}
                    </div>
                </div>
                <div class="col-md-4 text-md-end">
                    <div class="text-white">
                        <p class="mb-1">
                            <i class="fas fa-clock me-2"></i>
                            {{ job.created_at[:10] }}
                        </p>
                        {% if job.deadline %}
                        <p class="mb-0">
                            <i class="fas fa-calendar-alt me-2"></i>
                            ينتهي في: {{ job.deadline }}
                        </p>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container py-5">
        <div class="row">
            <!-- المحتوى الرئيسي -->
            <div class="col-lg-8">
                <div class="job-content">
                    <!-- معلومات الوظيفة -->
                    <div class="mb-5">
                        <h3 class="fw-bold mb-4 text-primary">
                            <i class="fas fa-info-circle me-2"></i>وصف الوظيفة
                        </h3>
                        <p class="fs-6 text-dark">{{ job.description }}</p>
                    </div>

                    <!-- المتطلبات -->
                    {% if job.requirements %}
                    <div class="mb-5">
                        <h3 class="fw-bold mb-4 text-primary">
                            <i class="fas fa-list-check me-2"></i>المتطلبات
                        </h3>
                        <div class="text-dark">
                            {{ job.requirements|replace('\n', '<br>')|safe }}
                        </div>
                    </div>
                    {% endif %}

                    <!-- المزايا -->
                    {% if job.benefits %}
                    <div class="mb-5">
                        <h3 class="fw-bold mb-4 text-primary">
                            <i class="fas fa-gift me-2"></i>المزايا
                        </h3>
                        <div class="text-dark">
                            {{ job.benefits|replace('\n', '<br>')|safe }}
                        </div>
                    </div>
                    {% endif %}
                </div>
            </div>

            <!-- الشريط الجانبي -->
            <div class="col-lg-4">
                <!-- معلومات الشركة -->
                <div class="company-card mb-4">
                    <h4 class="fw-bold mb-3">عن الشركة</h4>
                    <p class="text-muted mb-3">{{ job.company_description or 'شركة رائدة في السوق اليمني' }}</p>
                    
                    {% if job.company_phone %}
                    <p class="mb-2">
                        <i class="fas fa-phone me-2"></i>
                        {{ job.company_phone }}
                    </p>
                    {% endif %}
                    
                    {% if job.company_location %}
                    <p class="mb-0">
                        <i class="fas fa-map-marker-alt me-2"></i>
                        {{ job.company_location }}
                    </p>
                    {% endif %}
                </div>

                <!-- معلومات إضافية -->
                <div class="job-meta mb-4">
                    <h5 class="fw-bold mb-3">معلومات الوظيفة</h5>
                    
                    {% if job.salary_range %}
                    <div class="mb-3">
                        <strong>الراتب:</strong>
                        <p class="text-success mb-0">{{ job.salary_range }}</p>
                    </div>
                    {% endif %}
                    
                    {% if job.experience_level %}
                    <div class="mb-3">
                        <strong>مستوى الخبرة:</strong>
                        <p class="mb-0">{{ job.experience_level }}</p>
                    </div>
                    {% endif %}
                    
                    {% if job.deadline %}
                    <div class="mb-3">
                        <strong>موعد الانتهاء:</strong>
                        <p class="mb-0">{{ job.deadline }}</p>
                    </div>
                    {% endif %}
                </div>

                <!-- أزرار التقديم -->
                <div class="text-center">
                    <div class="d-grid gap-2">
                        <a href="/apply/{{ job.id }}" class="btn btn-primary btn-lg">
                            <i class="fas fa-paper-plane me-2"></i>تقديم عادي
                        </a>
                        
                        <!-- زر التقديم السريع -->
                        <a href="/mobile/apply/app?job_id={{ job.id }}&job_title={{ job.title|urlencode }}&company_name={{ job.company_name|urlencode }}&location={{ job.location|urlencode }}&job_type={{ job.job_type|urlencode }}" 
                           class="btn btn-success btn-lg quick-apply-btn">
                            <i class="fas fa-mobile-alt me-2"></i>📱 تقديم سريع
                        </a>
                        
                        <a href="/jobs" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-right me-2"></i>عرض المزيد من الوظائف
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- الفوتر -->
    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center">
                    <p class="mb-0">&copy; 2024 بوابتي للتوظيف. جميع الحقوق محفوظة.</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // كشف إذا كان المستخدم داخل التطبيق
            const isInApp = window.android || /MyJobPortalApp/i.test(navigator.userAgent);
            
            if (isInApp) {
                // تحديث رابط التقديم السريع
                const quickApplyBtn = document.querySelector('.quick-apply-btn');
                if (quickApplyBtn) {
                    let href = quickApplyBtn.getAttribute('href');
                    if (!href.includes('source=')) {
                        href += (href.includes('?') ? '&' : '?') + 'source=app';
                        quickApplyBtn.setAttribute('href', href);
                    }
                }
                
                // إضافة رسالة ترحيب
                console.log('🎯 مرحباً بك في تطبيق بوابتي للتوظيف!');
            }
        });
    </script>
</body>
</html>
